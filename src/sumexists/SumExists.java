package sumexists;

public class SumExists {
    public String sumOfTwoArrays(int[] arr, int target) {
        if(arr == null || arr.length <= 1) {
            return "No";
        }
        return "No";
    }
}
